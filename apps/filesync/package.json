{
  "name": "filesync",
  "version": "0.1.0",
  "description": "android sender app: send files to a desktop over the lan (localsend-compatible)",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "cap sync android",
    "copy": "cap copy android",
    "open": "cap open android",
    "build:apk": "cap sync android && cd android && ./gradlew assembleDebug"
  },
  "keywords": [
    "android",
    "capacitor",
    "localsend",
    "file-transfer"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
